<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Alex, Matt and Addison — September 25, 2026</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Italiana&family=Playfair+Display:ital,wght@0,400;0,500;0,700;1,400;1,500&family=Tangerine:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --maroon-deepest:  #2a0f14;
      --maroon-dark:     #4a1a24;
      --maroon-base:     #6b2533;
      --maroon-mid:      #8b3345;
      --maroon-light:    #a84a5a;
      --maroon-pale:     #d4949f;
      --maroon-whisper:  #f0dde1;

      --slate-deepest:   #1a2a3a;
      --slate-dark:      #2c4259;
      --slate-base:      #4a6a82;
      --slate-mid:       #6d8ea6;
      --slate-light:     #9fb8ca;
      --slate-pale:      #c8d9e4;
      --slate-whisper:   #e4edf3;

      --gold-accent:     #c4a265;
      --gold-dim:        #9e855a;

      --bg-page:         #1e1218;
      --bg-card:         rgba(30, 18, 24, 0.65);
      --bg-card-alt:     rgba(26, 42, 58, 0.25);
      --border-color:    rgba(196, 162, 101, 0.25);
      --border-fine:     rgba(196, 162, 101, 0.12);
      --text-primary:    #e8dde0;
      --text-secondary:  #c8b8bc;
      --text-muted:      #9a8a90;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background-color: var(--bg-page);
      background-image:
        radial-gradient(ellipse 80% 60% at 20% 10%, rgba(107, 37, 51, 0.2) 0%, transparent 60%),
        radial-gradient(ellipse 70% 50% at 80% 85%, rgba(44, 66, 89, 0.25) 0%, transparent 55%),
        radial-gradient(ellipse 40% 40% at 50% 50%, rgba(74, 106, 130, 0.08) 0%, transparent 70%);
      min-height: 100vh;
      font-family: 'Playfair Display', Georgia, serif;
      color: var(--text-primary);
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 3.5rem 1.5rem 6rem;
      position: relative;
      overflow-x: hidden;
    }

    /* Subtle repeating diamond pattern overlay */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(45deg, rgba(196, 162, 101, 0.02) 25%, transparent 25%),
        linear-gradient(-45deg, rgba(196, 162, 101, 0.02) 25%, transparent 25%),
        linear-gradient(45deg, transparent 75%, rgba(196, 162, 101, 0.02) 75%),
        linear-gradient(-45deg, transparent 75%, rgba(196, 162, 101, 0.02) 75%);
      background-size: 40px 40px;
      background-position: 0 0, 0 20px, 20px -20px, -20px 0px;
      pointer-events: none;
      z-index: 0;
    }

    /* ── Page wrapper ── */
    .page {
      max-width: 680px;
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 2rem;
      position: relative;
      z-index: 1;
    }

    /* ── Shared card shell ── */
    .card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 2px;
      position: relative;
      overflow: hidden;
      animation: fadeUp 0.8s ease both;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
    }
    .card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(170deg, rgba(196, 162, 101, 0.04) 0%, transparent 40%);
      pointer-events: none;
    }
    .card:nth-child(2) { animation-delay: 0.15s; }
    .card:nth-child(3) { animation-delay: 0.28s; }
    .card:nth-child(4) { animation-delay: 0.4s; }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(22px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* ── Corner geometric ornaments ── */
    .geo-corner {
      position: absolute;
      width: 90px;
      height: 90px;
      pointer-events: none;
      opacity: 0.35;
    }
    .geo-corner.tl { top: 0; left: 0; }
    .geo-corner.tr { top: 0; right: 0; transform: scaleX(-1); }
    .geo-corner.bl { bottom: 0; left: 0; transform: scaleY(-1); }
    .geo-corner.br { bottom: 0; right: 0; transform: scale(-1); }

    /* ── Inner border ── */
    .inner-frame {
      border: 1px solid var(--border-fine);
      margin: 1.5rem;
      padding: 2.8rem 2rem;
      position: relative;
      z-index: 1;
      text-align: center;
    }
    /* Gold corner ticks on inner frame */
    .inner-frame::before,
    .inner-frame::after {
      content: '';
      position: absolute;
      width: 20px;
      height: 20px;
      border-color: var(--gold-accent);
      opacity: 0.35;
    }
    .inner-frame::before {
      top: -1px; left: -1px;
      border-top: 1.5px solid;
      border-left: 1.5px solid;
    }
    .inner-frame::after {
      bottom: -1px; right: -1px;
      border-bottom: 1.5px solid;
      border-right: 1.5px solid;
    }
    /* Extra two corners via the parent */
    .card-has-corners .inner-frame {
      position: relative;
    }
    .corner-tr, .corner-bl {
      position: absolute;
      width: 20px;
      height: 20px;
      border-color: var(--gold-accent);
      opacity: 0.35;
      pointer-events: none;
      z-index: 2;
    }
    .corner-tr {
      top: calc(1.5rem - 1px);
      right: calc(1.5rem - 1px);
      border-top: 1.5px solid;
      border-right: 1.5px solid;
    }
    .corner-bl {
      bottom: calc(1.5rem - 1px);
      left: calc(1.5rem - 1px);
      border-bottom: 1.5px solid;
      border-left: 1.5px solid;
    }

    /* ── Typography ── */
    .label-sm {
      font-family: 'Italiana', serif;
      font-size: 0.82rem;
      letter-spacing: 0.32em;
      text-transform: uppercase;
      color: var(--gold-accent);
      margin-bottom: 0.35rem;
    }

    .tagline {
      font-size: 1.15rem;
      font-style: italic;
      color: var(--text-muted);
      margin-bottom: 2rem;
      font-weight: 400;
    }

    .name-first {
      font-family: 'Italiana', serif;
      font-size: 3.2rem;
      font-weight: 400;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--maroon-pale);
      line-height: 1;
    }

    .name-last {
      font-family: 'Playfair Display', serif;
      font-size: 1.15rem;
      font-weight: 400;
      font-style: italic;
      color: var(--slate-mid);
      letter-spacing: 0.1em;
      margin-top: 0.3rem;
    }

    .script-and {
      font-family: 'Tangerine', cursive;
      font-size: 4.4rem;
      font-weight: 700;
      color: var(--gold-dim);
      line-height: 1;
      margin: 0.7rem 0;
      display: block;
    }

    .divider {
      width: 80px;
      height: 1px;
      margin: 1.6rem auto;
      position: relative;
      overflow: visible;
    }
    .divider::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(90deg, transparent, var(--gold-accent), transparent);
      opacity: 0.35;
    }
    .divider::after {
      content: '';
      position: absolute;
      top: -2px;
      left: 50%;
      transform: translateX(-50%) rotate(45deg);
      width: 5px;
      height: 5px;
      background: var(--gold-accent);
      opacity: 0.3;
    }

    .date-display {
      display: flex;
      align-items: baseline;
      justify-content: center;
      gap: 0.6rem;
      margin: 0.4rem 0 1.4rem;
    }
    .date-month {
      font-family: 'Italiana', serif;
      font-size: 0.88rem;
      letter-spacing: 0.26em;
      text-transform: uppercase;
      color: var(--slate-light);
    }
    .date-day {
      font-family: 'Tangerine', cursive;
      font-size: 4.2rem;
      font-weight: 700;
      color: var(--maroon-light);
      line-height: 1;
    }
    .date-year {
      font-family: 'Italiana', serif;
      font-size: 1rem;
      letter-spacing: 0.12em;
      color: var(--slate-light);
    }

    .ceremony-line {
      font-size: 1.05rem;
      font-style: italic;
      color: var(--text-secondary);
      line-height: 1.8;
    }

    .venue-block {
      font-size: 1.1rem;
      font-style: italic;
      color: var(--text-muted);
      line-height: 1.8;
      margin-top: 0.3rem;
    }

    /* ── Vine divider ── */
    .vine-divider {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0.5rem 0;
      opacity: 0.4;
    }
    .vine-divider svg { width: 140px; height: 20px; }

    /* ── Details card ── */
    .back-card {
      background:
        linear-gradient(165deg, rgba(44, 66, 89, 0.3) 0%, rgba(30, 18, 24, 0.6) 100%);
      backdrop-filter: blur(12px);
    }

    .card-inner {
      padding: 2.5rem 3rem;
      text-align: center;
      position: relative;
      z-index: 1;
    }

    .section-title {
      font-family: 'Italiana', serif;
      font-size: .88rem;
      letter-spacing: 0.32em;
      text-transform: uppercase;
      color: var(--gold-accent);
      margin-bottom: 1.2rem;
    }

    .body-italic {
      font-size: 1.1rem;
      font-style: italic;
      font-weight: 400;
      color: var(--text-primary);
      line-height: 1.9;
    }

    .note-italic {
      font-size: 1rem;
      font-style: italic;
      color: var(--text-secondary);
      line-height: 1.85;
      margin-top: 1.2rem;
    }

    /* ── RSVP card ── */
    .rsvp-card {
      background: rgba(42, 15, 20, 0.5);
      backdrop-filter: blur(12px);
    }

    .rsvp-deadline {
      font-size: 1.05rem;
      font-style: italic;
      color: var(--text-secondary);
      line-height: 1.8;
    }

    .rsvp-phone {
      font-family: 'Italiana', serif;
      font-size: 1.7rem;
      font-weight: 400;
      letter-spacing: 0.08em;
      color: var(--maroon-pale);
      margin: 0.8rem 0 0.5rem;
    }

    .rsvp-instruction {
      font-size: 0.98rem;
      font-style: italic;
      color: var(--text-muted);
      line-height: 1.7;
    }

    /* ── Gift card ── */
    .gift-card {
      background: var(--bg-card);
      backdrop-filter: blur(12px);
    }

    /* ── Closing ── */
    .closing {
      text-align: center;
      font-family: 'Tangerine', cursive;
      font-size: 3rem;
      font-weight: 700;
      color: var(--gold-dim);
      margin-top: 1rem;
      animation: fadeUp 0.8s 0.55s ease both;
      position: relative;
      z-index: 1;
    }

    /* ── Responsive ── */
    @media (max-width: 520px) {
      .name-first { font-size: 2.2rem; letter-spacing: 0.14em; }
      .script-and { font-size: 3.4rem; }
      .date-day   { font-size: 3.2rem; }
      .card-inner, .inner-frame { padding-left: 1.2rem; padding-right: 1.2rem; }
    }
  </style>
</head>
<body>

<main class="page" role="main">

  <!-- ═══════════════════════════════════════════════
       CARD 1 — Invitation Front
  ═══════════════════════════════════════════════ -->
  <article class="card card-has-corners" aria-label="Wedding invitation front">

    <!-- Geometric corner ornaments -->
    <svg class="geo-corner tl" viewBox="0 0 90 90" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M0 0 L30 0 L30 4 L4 4 L4 30 L0 30 Z" fill="url(#gGold)" />
      <path d="M0 0 L18 0 L0 18 Z" fill="none" stroke="url(#gGold)" stroke-width="0.6" opacity="0.5"/>
      <line x1="8" y1="0" x2="0" y2="8" stroke="url(#gGold)" stroke-width="0.5" opacity="0.3"/>
      <circle cx="14" cy="14" r="1.5" fill="#c4a265" opacity="0.4"/>
      <defs>
        <linearGradient id="gGold" x1="0" y1="0" x2="30" y2="30">
          <stop offset="0%" stop-color="#c4a265" stop-opacity="0.7"/>
          <stop offset="100%" stop-color="#c4a265" stop-opacity="0.15"/>
        </linearGradient>
      </defs>
    </svg>
    <svg class="geo-corner tr" viewBox="0 0 90 90" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M0 0 L30 0 L30 4 L4 4 L4 30 L0 30 Z" fill="url(#gGold2)" />
      <path d="M0 0 L18 0 L0 18 Z" fill="none" stroke="url(#gGold2)" stroke-width="0.6" opacity="0.5"/>
      <circle cx="14" cy="14" r="1.5" fill="#c4a265" opacity="0.4"/>
      <defs>
        <linearGradient id="gGold2" x1="0" y1="0" x2="30" y2="30">
          <stop offset="0%" stop-color="#c4a265" stop-opacity="0.7"/>
          <stop offset="100%" stop-color="#c4a265" stop-opacity="0.15"/>
        </linearGradient>
      </defs>
    </svg>
    <svg class="geo-corner bl" viewBox="0 0 90 90" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M0 0 L30 0 L30 4 L4 4 L4 30 L0 30 Z" fill="url(#gGold3)" />
      <circle cx="14" cy="14" r="1.5" fill="#c4a265" opacity="0.4"/>
      <defs>
        <linearGradient id="gGold3" x1="0" y1="0" x2="30" y2="30">
          <stop offset="0%" stop-color="#c4a265" stop-opacity="0.7"/>
          <stop offset="100%" stop-color="#c4a265" stop-opacity="0.15"/>
        </linearGradient>
      </defs>
    </svg>
    <svg class="geo-corner br" viewBox="0 0 90 90" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M0 0 L30 0 L30 4 L4 4 L4 30 L0 30 Z" fill="url(#gGold4)" />
      <circle cx="14" cy="14" r="1.5" fill="#c4a265" opacity="0.4"/>
      <defs>
        <linearGradient id="gGold4" x1="0" y1="0" x2="30" y2="30">
          <stop offset="0%" stop-color="#c4a265" stop-opacity="0.7"/>
          <stop offset="100%" stop-color="#c4a265" stop-opacity="0.15"/>
        </linearGradient>
      </defs>
    </svg>

    <span class="corner-tr"></span>
    <span class="corner-bl"></span>

    <div class="inner-frame">
      <p class="label-sm">You Are Invited</p>
      <p class="tagline">to the wedding of</p>

      <div class="name-block">
        <div class="name-first">Alexandra</div>
        <div class="name-last">Lindstrom</div>
      </div>

      <span class="script-and">And</span>

      <div class="name-block">
        <div class="name-first">Matthew</div>
        <div class="name-last">Weigand</div>
      </div>

      <div class="divider"></div>

      <div class="date-display">
        <span class="date-month">Sept</span>
        <span class="date-day">25</span>
        <span class="date-year">2026</span>
      </div>

      <div class="divider"></div>

      <p class="ceremony-line">Ceremony starting at 5:00 PM</p>
      <p class="ceremony-line">Casual reception following immediately after until 10:00 PM</p>

      <div class="vine-divider" aria-hidden="true">
        <svg viewBox="0 0 140 20" fill="none" xmlns="http://www.w3.org/2000/svg">
          <line x1="0" y1="10" x2="55" y2="10" stroke="#c4a265" stroke-width="0.5" opacity="0.3"/>
          <line x1="85" y1="10" x2="140" y2="10" stroke="#c4a265" stroke-width="0.5" opacity="0.3"/>
          <path d="M58 10 L70 3 L82 10 L70 17 Z" stroke="#c4a265" stroke-width="0.7" fill="none" opacity="0.5"/>
          <path d="M63 10 L70 6 L77 10 L70 14 Z" stroke="#c4a265" stroke-width="0.5" fill="#c4a265" opacity="0.15"/>
          <circle cx="70" cy="10" r="1.5" fill="#c4a265" opacity="0.4"/>
        </svg>
      </div>

      <p class="venue-block">
        South Shore Pavilion<br>
        2900 S Shore Dr.<br>
        Milwaukee, WI 53207
      </p>
    </div>
  </article>

  <!-- ═══════════════════════════════════════════════
       CARD 2 — Details (Back of card)
  ═══════════════════════════════════════════════ -->
  <article class="card back-card" aria-label="Wedding details">
    <div class="card-inner">
      <p class="label-sm" style="margin-bottom:0.3rem">We Hope You Can Join Us!</p>

      <div class="divider" style="margin-top:1rem"></div>

      <p class="section-title">Details</p>
      <p class="body-italic">
        Casual attire. <br>
        The night will include appetizers, drinks,<br>
        a buffet dinner, and dancing.<br>
        Feel free to come and go as you please!
      </p>
    </div>
  </article>

  <!-- ═══════════════════════════════════════════════
       CARD 3 — RSVP
  ═══════════════════════════════════════════════ -->
  <article class="card rsvp-card" aria-label="RSVP information">
    <div class="card-inner">
      <p class="section-title">RSVP</p>
      <p class="rsvp-deadline">
        Please RSVP by August 9th, 2026<br>
        via text message to Alex at
      </p>
      <p class="rsvp-phone">(414) 828-7999</p>
      <p class="rsvp-instruction">
        In your RSVP, please include the names of guests attending.
      </p>

      <div class="divider"></div>
    </div>
  </article>

  <!-- ═══════════════════════════════════════════════
       CARD 4 — Gift Note
  ═══════════════════════════════════════════════ -->
  <article class="card gift-card" aria-label="Gift preferences">
    <div class="card-inner">
      <p class="body-italic">
        We're so excited to celebrate with you!
      </p>
      <p class="note-italic">
        If you're considering a gift to help us on our way,
        in place of physical gifts, we would find a monetary
        contribution toward our future together most helpful.
      </p>
    </div>
  </article>

</main>

<p class="closing" aria-label="Closing">With Love</p>

</body>
</html>